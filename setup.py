from setuptools import setup
setup(name="pypty", version=2.0.0, description="A CuPy-based set of scripts for multisclie reconstrcutions on a GPU. Can be configured for STEM (ptychograpgy), TEM (tilt or focal series) or pretty much any combination of this things.", packages=['pypty'], author="Anton Gladyshev",zip_safe=False)

